name: test
on: push
jobs:
        test:
                runs-on: ubuntu-20.04
                steps:
                        - uses: actions/checkout@3
                        - name: All test
                          run: bash -xv ./test.bash
